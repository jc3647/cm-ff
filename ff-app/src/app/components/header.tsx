import React from 'react';

type Props = {
  name: string;
};

const Header: React.FC<Props> = ({ name }) => {
  return <h1>Under construction... Chode Mongers FF website</h1>;
};

export default Header;